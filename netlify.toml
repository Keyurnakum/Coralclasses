[build]
command = "npm run build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

# API proxy (optional - if using backend)
[[redirects]]
from = "/api/*"
to = "http://your-backend-url/api/:splat"
status = 200
force = false
